---
type Link = {
  label: string;
  href: string;
};

type Props = {
  title: string;
  description: string;
  awards?: string[];
  venue?: string;
  year?: string | number;
  authors?: string[];
  image?: string;
  imageAlt?: string;
  tags?: string[];
  links?: Link[];
};

const {
  title,
  description,
  awards = [],
  venue,
  year,
  authors = [],
  image,
  imageAlt = "",
  tags = [],
  links = [],
} = Astro.props;
---

<article class="research-card group overflow-hidden transition">
  <div
    class:list={[
      image
        ? "grid items-start gap-5 md:grid-cols-[320px_minmax(0,1fr)] md:gap-6"
        : "block",
    ]}
  >
    {
      image && (
        <div class="flex w-full max-w-[340px] self-start items-start justify-start rounded-lg bg-white py-3 pr-3 md:max-w-none">
          <img
            src={image}
            alt={imageAlt || title}
            class="aspect-[3/2] w-full rounded-md object-contain"
            loading="lazy"
          />
        </div>
      )
    }

    <div class="min-w-0 space-y-2.5 py-1 md:pr-3">
      <div class="space-y-2">
        <div class="flex flex-wrap items-center gap-2 text-xs">
          {venue && (
            <span
              class:list={[
                "research-venue",
              ]}
            >
              {venue}
            </span>
          )}

          {year && (
            <span class="text-neutral-500 dark:text-neutral-400">
              {year}
            </span>
          )}
        {awards.length > 0 && (
            <div class="flex flex-wrap items-center gap-1.5">
            {awards.map((award) => (
                <span
                    class="inline-flex items-center rounded-full border border-emerald-200 bg-emerald-50 px-2.5 py-1 text-xs font-medium text-emerald-700 dark:border-emerald-900/70 dark:bg-emerald-950/40 dark:text-emerald-300"
                >
                    {award}
                </span>
            ))}
            </div>
        )}
        </div>

        <h2 class="research-title">
          {title}
        </h2>

        {
          authors.length > 0 && (
            <p class="text-sm leading-6 text-neutral-500 dark:text-neutral-400">
              {authors.map((author, index) => (
                <>
                  {author.includes("E. Lin") ? (
                    <strong class="font-semibold text-neutral-800 dark:text-neutral-200">
                      {author}
                    </strong>
                  ) : (
                    <span>{author}</span>
                  )}
                  {index < authors.length - 1 && <span>, </span>}
                </>
              ))}
            </p>
          )
        }

        {description && (
          <p class="text-sm leading-6 text-neutral-600 dark:text-neutral-300">
            {description}
          </p>
        )}
      </div>

      {
        tags.length > 0 && (
          <div class="flex flex-wrap gap-2">
            {tags.map((tag) => (
              <span class="rounded-full bg-neutral-100 px-2.5 py-1 text-xs text-neutral-700 dark:bg-neutral-800 dark:text-neutral-300">
                {tag}
              </span>
            ))}
          </div>
        )
      }

      {
        links.length > 0 && (
          <div class="flex flex-wrap gap-2 text-sm">
            {links.map((link) => (
              <a
                href={link.href}
                class="research-link"
                target={link.href.startsWith("http") ? "_blank" : undefined}
                rel={link.href.startsWith("http") ? "noreferrer" : undefined}
              >
                {link.label}
              </a>
            ))}
          </div>
        )
      }
    </div>
  </div>
</article>
