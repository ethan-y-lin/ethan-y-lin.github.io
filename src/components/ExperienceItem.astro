---
type Props = {
  role: string;
  organization: string;
  date: string;
  location?: string;
  description?: string;
};

const { role, organization, date, location, description } = Astro.props;
---

<div class="rounded-2xl border border-neutral-200 bg-white p-5 dark:border-neutral-800 dark:bg-neutral-950">
  <div class="flex flex-col gap-1 md:flex-row md:items-start md:justify-between">
    <div>
      <h3 class="font-semibold text-neutral-900 dark:text-neutral-100">{role}</h3>
      <p class="text-sm text-neutral-500 dark:text-neutral-400">
        {organization}{location ? ` · ${location}` : ""}
      </p>
    </div>

    <p class="text-sm text-neutral-500 dark:text-neutral-500">{date}</p>
  </div>

  {description && (
    <p class="mt-3 text-sm leading-6 text-neutral-600 dark:text-neutral-300">
      {description}
    </p>
  )}
</div>